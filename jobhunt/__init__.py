"""MyJobAgent — local-first job-hunting agent.

The web only configures and generates a config; the engine runs on the user's
machine with their own LLM. Nothing leaves the machine by default.
"""

__version__ = "0.1.0a0"
