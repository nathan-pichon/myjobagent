---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <!-- ===================== HEADER ===================== -->
  <header style="background:var(--jb-surface);border-bottom:1px solid var(--jb-border);position:sticky;top:0;z-index:50;">
    <div style="max-width:1100px;margin:0 auto;padding:0 1.25rem;display:flex;align-items:center;justify-content:space-between;height:3.5rem;">
      <div style="display:flex;align-items:center;gap:0.625rem;">
        <svg width="28" height="28" viewBox="0 0 32 32" fill="none" aria-hidden="true">
          <defs>
            <linearGradient id="hLogo" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stop-color="#2E6BFF"/>
              <stop offset="100%" stop-color="#00C2A8"/>
            </linearGradient>
          </defs>
          <circle cx="16" cy="16" r="14" fill="url(#hLogo)"/>
          <circle cx="16" cy="16" r="5" fill="white" opacity="0.9"/>
          <circle cx="16" cy="16" r="2" fill="url(#hLogo)"/>
        </svg>
        <span style="font-weight:700;font-size:1.0625rem;color:var(--jb-text);">MyJobAgent</span>
      </div>

      <span class="privacy-badge" aria-label="Confidentialité : tout reste sur ta machine">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        100 % local — rien n'est envoyé
      </span>
    </div>
  </header>

  <main>
    <!-- ===================== HERO ===================== -->
    <section aria-labelledby="hero-title" style="overflow:hidden;position:relative;">
      <!-- Beam background -->
      <div style="position:absolute;inset:0;background:var(--jb-beam);opacity:0.07;" aria-hidden="true"></div>
      <!-- Beam orb -->
      <div style="position:absolute;top:-4rem;right:-4rem;width:22rem;height:22rem;background:radial-gradient(circle,rgba(46,107,255,.15) 0%,transparent 70%);pointer-events:none;" aria-hidden="true"></div>

      <div style="position:relative;max-width:1100px;margin:0 auto;padding:4rem 1.25rem 3.5rem;text-align:center;">
        <p style="display:inline-block;font-size:0.75rem;font-weight:700;letter-spacing:0.08em;text-transform:uppercase;color:var(--jb-accent-text);background:var(--jb-accent-bg);border-radius:999px;padding:0.25rem 0.875rem;margin-bottom:1.25rem;">
          Agent IA local · Open Source
        </p>

        <h1 id="hero-title" style="font-size:clamp(2rem,5vw,3.25rem);font-weight:700;line-height:1.15;color:var(--jb-text);max-width:680px;margin:0 auto 1.25rem;">
          Décris ton job idéal une fois.<br>
          <span style="background:var(--jb-beam);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;">Ton agent le cherche en local</span><br>
          et t'explique chaque score.
        </h1>

        <p style="font-size:1.0625rem;color:var(--jb-text-muted);max-width:520px;margin:0 auto 2rem;line-height:1.65;">
          MyJobAgent scanne les plateformes d'emploi avec un LLM local,
          score chaque offre sur 100 points, et génère un tableau de bord privé — sans envoyer tes données nulle part.
        </p>

        <div style="display:flex;flex-wrap:wrap;gap:0.875rem;justify-content:center;align-items:center;">
          <a href="#wizard" class="jb-btn-primary" style="font-size:1rem;padding:0.75rem 2rem;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
            Créer mon agent
          </a>
          <a href="https://github.com" class="jb-btn-secondary" target="_blank" rel="noopener noreferrer">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0 1 12 6.844a9.59 9.59 0 0 1 2.504.337c1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.942.359.31.678.921.678 1.856 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.02 10.02 0 0 0 22 12.017C22 6.484 17.522 2 12 2z"/></svg>
            Voir le code
          </a>
        </div>

        <!-- How it works - 3 steps -->
        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1.25rem;margin-top:3.5rem;text-align:left;">
          <div class="jb-card" style="padding:1.25rem;">
            <div style="width:2.25rem;height:2.25rem;background:var(--jb-primary-soft);border-radius:var(--jb-radius-input);display:flex;align-items:center;justify-content:center;margin-bottom:0.875rem;">
              <span style="color:var(--jb-primary);font-weight:700;font-size:0.9375rem;">1</span>
            </div>
            <h3 style="font-size:0.9375rem;font-weight:600;color:var(--jb-text);margin-bottom:0.375rem;">Configure ton profil</h3>
            <p style="font-size:0.8125rem;color:var(--jb-text-muted);line-height:1.55;">Rôles, stack, lieu, contrat, salaire min. — tout dans un formulaire guidé.</p>
          </div>
          <div class="jb-card" style="padding:1.25rem;">
            <div style="width:2.25rem;height:2.25rem;background:var(--jb-accent-bg);border-radius:var(--jb-radius-input);display:flex;align-items:center;justify-content:center;margin-bottom:0.875rem;">
              <span style="color:var(--jb-accent-text);font-weight:700;font-size:0.9375rem;">2</span>
            </div>
            <h3 style="font-size:0.9375rem;font-weight:600;color:var(--jb-text);margin-bottom:0.375rem;">Lance l'agent en local</h3>
            <p style="font-size:0.8125rem;color:var(--jb-text-muted);line-height:1.55;">Télécharge ta config, exécute <code style="font-family:monospace;font-size:0.75rem;background:var(--jb-surface-alt);padding:0.1em 0.3em;border-radius:4px;">mja run</code>. L'agent scanne pour toi.</p>
          </div>
          <div class="jb-card" style="padding:1.25rem;">
            <div style="width:2.25rem;height:2.25rem;background:var(--jb-signal);border-radius:var(--jb-radius-input);display:flex;align-items:center;justify-content:center;margin-bottom:0.875rem;">
              <span style="color:var(--jb-signal-text);font-weight:700;font-size:0.9375rem;">3</span>
            </div>
            <h3 style="font-size:0.9375rem;font-weight:600;color:var(--jb-text);margin-bottom:0.375rem;">Explore les matches</h3>
            <p style="font-size:0.8125rem;color:var(--jb-text-muted);line-height:1.55;">Tableau de bord local avec score détaillé — jamais envoyé nulle part.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ===================== WIZARD ===================== -->
    <section id="wizard" aria-labelledby="wizard-title" style="max-width:1100px;margin:0 auto;padding:3rem 1.25rem 5rem;">
      <div style="text-align:center;margin-bottom:2.5rem;">
        <h2 id="wizard-title" style="font-size:1.875rem;font-weight:700;color:var(--jb-text);margin-bottom:0.625rem;">Créer ta configuration</h2>
        <p style="color:var(--jb-text-muted);font-size:0.9375rem;">Complète les étapes ci-dessous. Ta config est générée dans ton navigateur, rien n'est envoyé.</p>
      </div>

      <!-- Main wizard container -->
      <div id="wizard-app" style="display:grid;grid-template-columns:1fr;gap:1.5rem;">
        <!-- Step navigation -->
        <nav aria-label="Étapes du configurateur" style="display:flex;align-items:center;justify-content:center;gap:0.875rem;flex-wrap:wrap;">
          <button class="step-btn active" data-step="0" aria-current="step" style="display:flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;border-radius:var(--jb-radius-pill);border:1px solid var(--jb-primary);background:var(--jb-primary-soft);color:var(--jb-primary);font-weight:600;font-size:0.8125rem;cursor:pointer;">
            <span class="step-num" style="width:1.25rem;height:1.25rem;background:var(--jb-primary);color:white;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.6875rem;font-weight:700;">1</span>
            Profil
          </button>
          <div style="width:1.5rem;height:1px;background:var(--jb-border);flex-shrink:0;" aria-hidden="true"></div>
          <button class="step-btn" data-step="1" style="display:flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;border-radius:var(--jb-radius-pill);border:1px solid var(--jb-border);background:var(--jb-surface);color:var(--jb-text-muted);font-size:0.8125rem;cursor:pointer;">
            <span class="step-num" style="width:1.25rem;height:1.25rem;background:var(--jb-border);color:var(--jb-text-muted);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.6875rem;font-weight:700;">2</span>
            Recherche
          </button>
          <div style="width:1.5rem;height:1px;background:var(--jb-border);flex-shrink:0;" aria-hidden="true"></div>
          <button class="step-btn" data-step="2" style="display:flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;border-radius:var(--jb-radius-pill);border:1px solid var(--jb-border);background:var(--jb-surface);color:var(--jb-text-muted);font-size:0.8125rem;cursor:pointer;">
            <span class="step-num" style="width:1.25rem;height:1.25rem;background:var(--jb-border);color:var(--jb-text-muted);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.6875rem;font-weight:700;">3</span>
            LLM
          </button>
          <div style="width:1.5rem;height:1px;background:var(--jb-border);flex-shrink:0;" aria-hidden="true"></div>
          <button class="step-btn" data-step="3" style="display:flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;border-radius:var(--jb-radius-pill);border:1px solid var(--jb-border);background:var(--jb-surface);color:var(--jb-text-muted);font-size:0.8125rem;cursor:pointer;">
            <span class="step-num" style="width:1.25rem;height:1.25rem;background:var(--jb-border);color:var(--jb-text-muted);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.6875rem;font-weight:700;">4</span>
            Sources
          </button>
          <div style="width:1.5rem;height:1px;background:var(--jb-border);flex-shrink:0;" aria-hidden="true"></div>
          <button class="step-btn" data-step="4" style="display:flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;border-radius:var(--jb-radius-pill);border:1px solid var(--jb-border);background:var(--jb-surface);color:var(--jb-text-muted);font-size:0.8125rem;cursor:pointer;">
            <span class="step-num" style="width:1.25rem;height:1.25rem;background:var(--jb-border);color:var(--jb-text-muted);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:0.6875rem;font-weight:700;">5</span>
            Générer
          </button>
        </nav>

        <!-- Two-column layout on desktop: form + preview -->
        <div id="wizard-columns" style="display:grid;grid-template-columns:1fr;gap:1.5rem;">

          <!-- Form area -->
          <div id="wizard-form" class="jb-card" style="padding:1.75rem;">

            <!-- === STEP 1: Profil === -->
            <div class="wizard-step" data-step="0">
              <h3 style="font-size:1.125rem;font-weight:700;color:var(--jb-text);margin-bottom:1.5rem;">Ton profil de recherche</h3>

              <div style="display:grid;gap:1.25rem;">

                <!-- Roles -->
                <div>
                  <label class="jb-label" for="roles-input">Rôles recherchés <span style="color:var(--jb-danger)">*</span></label>
                  <div id="roles-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="roles-input" type="text" placeholder="Ex : Senior Backend Engineer — Entrée pour ajouter" aria-describedby="roles-hint" style="flex:1;" />
                    <button type="button" id="roles-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                  <p id="roles-hint" style="font-size:0.75rem;color:var(--jb-text-muted);margin-top:0.375rem;">Suggestions : Senior Backend Engineer · Tech Lead · Backend Architect · Fractional CTO</p>
                  <div style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-top:0.5rem;" aria-hidden="true">
                    <button type="button" class="suggestion-btn" data-target="roles" data-value="Senior Backend Engineer" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Senior Backend Engineer</button>
                    <button type="button" class="suggestion-btn" data-target="roles" data-value="Tech Lead Backend" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Tech Lead Backend</button>
                    <button type="button" class="suggestion-btn" data-target="roles" data-value="Backend Architect" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Backend Architect</button>
                    <button type="button" class="suggestion-btn" data-target="roles" data-value="Fractional CTO" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Fractional CTO</button>
                    <button type="button" class="suggestion-btn" data-target="roles" data-value="Principal Engineer" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Principal Engineer</button>
                  </div>
                </div>

                <!-- Stack core -->
                <div>
                  <label class="jb-label" for="stack-core-input">Stack indispensable (must-have)</label>
                  <div id="stack-core-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="stack-core-input" type="text" placeholder="Ex : Node.js, TypeScript…" style="flex:1;" />
                    <button type="button" id="stack-core-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                  <div style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-top:0.5rem;" aria-hidden="true">
                    <button type="button" class="suggestion-btn" data-target="stack-core" data-value="Node.js" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Node.js</button>
                    <button type="button" class="suggestion-btn" data-target="stack-core" data-value="TypeScript" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">TypeScript</button>
                    <button type="button" class="suggestion-btn" data-target="stack-core" data-value="Python" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Python</button>
                    <button type="button" class="suggestion-btn" data-target="stack-core" data-value="Go" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Go</button>
                    <button type="button" class="suggestion-btn" data-target="stack-core" data-value="Rust" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Rust</button>
                  </div>
                </div>

                <!-- Stack nice -->
                <div>
                  <label class="jb-label" for="stack-nice-input">Stack nice-to-have (bonus)</label>
                  <div id="stack-nice-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="stack-nice-input" type="text" placeholder="Ex : Docker, Kubernetes, MongoDB…" style="flex:1;" />
                    <button type="button" id="stack-nice-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                </div>

                <!-- Exclusions -->
                <div>
                  <label class="jb-label" for="exclusions-input">Exclusions (hard stop)</label>
                  <div id="exclusions-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="exclusions-input" type="text" placeholder="Ex : PHP, on-site only…" style="flex:1;" />
                    <button type="button" id="exclusions-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                </div>

                <!-- Geography -->
                <div>
                  <label class="jb-label" for="geography-input">Lieux / Géographie</label>
                  <div id="geography-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="geography-input" type="text" placeholder="Ex : Nice, Remote, Bordeaux…" style="flex:1;" />
                    <button type="button" id="geography-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                  <div style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-top:0.5rem;" aria-hidden="true">
                    <button type="button" class="suggestion-btn" data-target="geography" data-value="Remote" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Remote</button>
                    <button type="button" class="suggestion-btn" data-target="geography" data-value="Full Remote" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Full Remote</button>
                    <button type="button" class="suggestion-btn" data-target="geography" data-value="Paris" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Paris</button>
                    <button type="button" class="suggestion-btn" data-target="geography" data-value="Nice" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Nice</button>
                    <button type="button" class="suggestion-btn" data-target="geography" data-value="Lyon" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">Lyon</button>
                  </div>
                </div>

                <!-- Contract types -->
                <div>
                  <fieldset style="border:none;padding:0;margin:0;">
                    <legend class="jb-label">Types de contrat</legend>
                    <div style="display:flex;flex-wrap:wrap;gap:0.75rem;margin-top:0.5rem;">
                      <label style="display:flex;align-items:center;gap:0.5rem;cursor:pointer;font-size:0.875rem;">
                        <input type="checkbox" id="ct-cdi" data-contract="CDI" class="contract-cb" style="accent-color:var(--jb-primary);width:1rem;height:1rem;" />
                        CDI
                      </label>
                      <label style="display:flex;align-items:center;gap:0.5rem;cursor:pointer;font-size:0.875rem;">
                        <input type="checkbox" id="ct-freelance" data-contract="Freelance" class="contract-cb" style="accent-color:var(--jb-primary);width:1rem;height:1rem;" />
                        Freelance
                      </label>
                      <label style="display:flex;align-items:center;gap:0.5rem;cursor:pointer;font-size:0.875rem;">
                        <input type="checkbox" id="ct-mission" data-contract="Mission" class="contract-cb" style="accent-color:var(--jb-primary);width:1rem;height:1rem;" />
                        Mission
                      </label>
                      <label style="display:flex;align-items:center;gap:0.5rem;cursor:pointer;font-size:0.875rem;">
                        <input type="checkbox" id="ct-cdd" data-contract="CDD" class="contract-cb" style="accent-color:var(--jb-primary);width:1rem;height:1rem;" />
                        CDD
                      </label>
                    </div>
                  </fieldset>
                </div>

                <!-- Seniority -->
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:1rem;">
                  <div>
                    <label class="jb-label" for="seniority-select">Séniorité</label>
                    <select id="seniority-select" class="jb-input" style="cursor:pointer;">
                      <option value="Junior">Junior</option>
                      <option value="Mid" >Mid</option>
                      <option value="Senior" selected>Senior</option>
                      <option value="Staff">Staff</option>
                      <option value="Principal">Principal</option>
                    </select>
                  </div>
                  <div>
                    <label class="jb-label" for="years-exp">Années d'exp. (optionnel)</label>
                    <input class="jb-input" id="years-exp" type="number" min="0" max="40" placeholder="Ex : 8" />
                  </div>
                </div>

                <!-- Salary / TJM -->
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:1rem;">
                  <div>
                    <label class="jb-label" for="min-salary">Salaire min CDI (€/an, optionnel)</label>
                    <input class="jb-input" id="min-salary" type="number" min="0" step="1000" placeholder="Ex : 65000" />
                  </div>
                  <div>
                    <label class="jb-label" for="min-tjm">TJM min freelance (€/jour, optionnel)</label>
                    <input class="jb-input" id="min-tjm" type="number" min="0" step="50" placeholder="Ex : 600" />
                  </div>
                </div>

              </div>

              <div style="display:flex;justify-content:flex-end;margin-top:2rem;">
                <button type="button" class="jb-btn-primary next-step" data-next="1">
                  Suivant : Recherche
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
                </button>
              </div>
            </div>

            <!-- === STEP 2: Recherche === -->
            <div class="wizard-step" data-step="1" style="display:none;">
              <h3 style="font-size:1.125rem;font-weight:700;color:var(--jb-text);margin-bottom:1.5rem;">Paramètres de recherche</h3>

              <div style="display:grid;gap:1.5rem;">

                <!-- Search modes -->
                <div>
                  <fieldset style="border:none;padding:0;margin:0;">
                    <legend class="jb-label">Modes de recherche</legend>
                    <div style="display:grid;gap:0.625rem;margin-top:0.5rem;">
                      <label style="display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer;padding:0.75rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);">
                        <input type="checkbox" id="mode-platform" data-mode="PLATFORM" class="mode-cb" checked style="accent-color:var(--jb-primary);width:1rem;height:1rem;margin-top:0.125rem;flex-shrink:0;" />
                        <div>
                          <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">PLATFORM</div>
                          <div style="font-size:0.75rem;color:var(--jb-text-muted);">Recherche ciblée sur les plateformes d'offres (WelcomeToTheJungle, Free-Work, Malt…)</div>
                        </div>
                      </label>
                      <label style="display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer;padding:0.75rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);">
                        <input type="checkbox" id="mode-careers" data-mode="CAREERS" class="mode-cb" checked style="accent-color:var(--jb-primary);width:1rem;height:1rem;margin-top:0.125rem;flex-shrink:0;" />
                        <div>
                          <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">CAREERS</div>
                          <div style="font-size:0.75rem;color:var(--jb-text-muted);">Pages carrières des entreprises directement (recrutement Node.js Nice)</div>
                        </div>
                      </label>
                      <label style="display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer;padding:0.75rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);">
                        <input type="checkbox" id="mode-linkedin" data-mode="LINKEDIN" class="mode-cb" style="accent-color:var(--jb-primary);width:1rem;height:1rem;margin-top:0.125rem;flex-shrink:0;" />
                        <div>
                          <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">LINKEDIN</div>
                          <div style="font-size:0.75rem;color:var(--jb-text-muted);">Recherche sur LinkedIn Jobs (peut être bloqué selon les CGU)</div>
                        </div>
                      </label>
                    </div>
                  </fieldset>
                </div>

                <!-- Platforms -->
                <div>
                  <label class="jb-label" for="platforms-input">Plateformes cibles</label>
                  <div id="platforms-tags" style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-bottom:0.5rem;" aria-live="polite"></div>
                  <div style="display:flex;gap:0.5rem;">
                    <input class="jb-input" id="platforms-input" type="text" placeholder="Ex : talent.io" style="flex:1;" />
                    <button type="button" id="platforms-add" class="jb-btn-secondary" style="white-space:nowrap;padding:0.5rem 0.875rem;border-radius:var(--jb-radius-input);">+ Ajouter</button>
                  </div>
                  <div style="display:flex;flex-wrap:wrap;gap:0.375rem;margin-top:0.5rem;" aria-hidden="true">
                    <button type="button" class="suggestion-btn" data-target="platforms" data-value="welcometothejungle.com" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">welcometothejungle.com</button>
                    <button type="button" class="suggestion-btn" data-target="platforms" data-value="free-work.com" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">free-work.com</button>
                    <button type="button" class="suggestion-btn" data-target="platforms" data-value="malt.fr" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">malt.fr</button>
                    <button type="button" class="suggestion-btn" data-target="platforms" data-value="talent.io" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">talent.io</button>
                    <button type="button" class="suggestion-btn" data-target="platforms" data-value="lesjeudis.com" style="font-size:0.6875rem;padding:0.15rem 0.5rem;border:1px solid var(--jb-border);border-radius:999px;background:var(--jb-surface-alt);color:var(--jb-text-muted);cursor:pointer;">lesjeudis.com</button>
                  </div>
                </div>

                <!-- Score threshold -->
                <div>
                  <label class="jb-label" for="score-threshold">Score minimum (0–100) : <strong id="threshold-display">50</strong></label>
                  <input type="range" id="score-threshold" min="0" max="100" value="50" step="5"
                    style="width:100%;accent-color:var(--jb-primary);cursor:pointer;"
                    aria-describedby="threshold-hint"
                  />
                  <div id="threshold-hint" style="display:flex;justify-content:space-between;font-size:0.6875rem;color:var(--jb-text-muted);margin-top:0.25rem;">
                    <span>0 — tout voir</span>
                    <span>50 — recommandé</span>
                    <span>100 — exact</span>
                  </div>
                </div>

              </div>

              <div style="display:flex;justify-content:space-between;margin-top:2rem;">
                <button type="button" class="jb-btn-secondary prev-step" data-prev="0">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg>
                  Retour
                </button>
                <button type="button" class="jb-btn-primary next-step" data-next="2">
                  Suivant : LLM
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
                </button>
              </div>
            </div>

            <!-- === STEP 3: LLM === -->
            <div class="wizard-step" data-step="2" style="display:none;">
              <h3 style="font-size:1.125rem;font-weight:700;color:var(--jb-text);margin-bottom:0.5rem;">Modèle de langage (LLM)</h3>
              <p style="font-size:0.8125rem;color:var(--jb-text-muted);margin-bottom:1.5rem;">L'agent utilise ce LLM en local pour comprendre et scorer les offres.</p>

              <!-- Privacy badge in LLM step (spec §8.8) -->
              <div style="display:flex;align-items:flex-start;gap:0.75rem;padding:0.875rem 1rem;background:var(--jb-accent-bg);border:1px solid var(--jb-accent);border-radius:var(--jb-radius-input);margin-bottom:1.5rem;">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--jb-accent-text)" stroke-width="2.5" aria-hidden="true" style="flex-shrink:0;margin-top:0.125rem;"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <div>
                  <p style="font-size:0.8125rem;font-weight:600;color:var(--jb-accent-text);margin:0 0 0.25rem;">Ta clé API se configure en local — jamais ici.</p>
                  <p style="font-size:0.75rem;color:var(--jb-accent-text);margin:0;">Utilise la variable d'environnement <code style="font-family:monospace;background:var(--jb-accent-bg);padding:0.05em 0.3em;border-radius:3px;">JOBHUNT_LLM_API_KEY</code> ou le trousseau système. Ta clé ne transite jamais par nos serveurs.</p>
                </div>
              </div>

              <div style="display:grid;gap:1.25rem;">

                <!-- Provider cards -->
                <div>
                  <fieldset style="border:none;padding:0;margin:0;">
                    <legend class="jb-label">Fournisseur LLM</legend>
                    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:0.625rem;margin-top:0.5rem;">

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="ollama" checked class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card active" data-provider="ollama" style="padding:0.875rem;border:2px solid var(--jb-primary);border-radius:var(--jb-radius-input);background:var(--jb-primary-soft);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-primary);margin-bottom:0.25rem;">Ollama</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">Recommandé — 100% local</div>
                          <div style="margin-top:0.375rem;font-size:0.625rem;background:var(--jb-primary);color:white;border-radius:999px;padding:0.1em 0.5em;display:inline-block;">Pas de clé</div>
                        </div>
                      </label>

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="lmstudio" class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card" data-provider="lmstudio" style="padding:0.875rem;border:2px solid var(--jb-border);border-radius:var(--jb-radius-input);background:var(--jb-surface);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-text);margin-bottom:0.25rem;">LM Studio</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">Local, interface graphique</div>
                        </div>
                      </label>

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="openai" class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card" data-provider="openai" style="padding:0.875rem;border:2px solid var(--jb-border);border-radius:var(--jb-radius-input);background:var(--jb-surface);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-text);margin-bottom:0.25rem;">OpenAI</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">GPT-4o / GPT-4 mini</div>
                        </div>
                      </label>

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="anthropic" class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card" data-provider="anthropic" style="padding:0.875rem;border:2px solid var(--jb-border);border-radius:var(--jb-radius-input);background:var(--jb-surface);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-text);margin-bottom:0.25rem;">Anthropic</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">Claude Haiku / Sonnet</div>
                        </div>
                      </label>

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="mistral" class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card" data-provider="mistral" style="padding:0.875rem;border:2px solid var(--jb-border);border-radius:var(--jb-radius-input);background:var(--jb-surface);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-text);margin-bottom:0.25rem;">Mistral</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">Mistral 7B / Small</div>
                        </div>
                      </label>

                      <label class="llm-card-label" style="cursor:pointer;">
                        <input type="radio" name="llm-provider" value="groq" class="llm-provider-radio" style="position:absolute;opacity:0;pointer-events:none;" />
                        <div class="llm-card" data-provider="groq" style="padding:0.875rem;border:2px solid var(--jb-border);border-radius:var(--jb-radius-input);background:var(--jb-surface);">
                          <div style="font-weight:700;font-size:0.875rem;color:var(--jb-text);margin-bottom:0.25rem;">Groq</div>
                          <div style="font-size:0.6875rem;color:var(--jb-text-muted);">Très rapide, gratuit</div>
                        </div>
                      </label>

                    </div>
                  </fieldset>
                </div>

                <!-- Model name -->
                <div>
                  <label class="jb-label" for="llm-model">Nom du modèle</label>
                  <input class="jb-input" id="llm-model" type="text" value="gemma4:e2b" placeholder="gemma4:e2b" />
                  <p id="llm-model-hint" style="font-size:0.75rem;color:var(--jb-text-muted);margin-top:0.375rem;">Pour Ollama : <code style="font-family:monospace;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">gemma4:e2b</code> (léger), <code style="font-family:monospace;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">gemma4:e4b</code>, <code style="font-family:monospace;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">qwen2.5:7b</code></p>
                </div>

                <!-- Base URL (advanced) -->
                <details style="border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);overflow:hidden;">
                  <summary style="padding:0.75rem;cursor:pointer;font-size:0.8125rem;font-weight:600;color:var(--jb-text);list-style:none;display:flex;align-items:center;justify-content:space-between;">
                    Paramètres avancés (URL de base)
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
                  </summary>
                  <div style="padding:0.75rem;border-top:1px solid var(--jb-border);">
                    <label class="jb-label" for="llm-base-url">URL de base du LLM</label>
                    <input class="jb-input" id="llm-base-url" type="url" value="http://localhost:11434" placeholder="http://localhost:11434" />
                    <p style="font-size:0.75rem;color:var(--jb-text-muted);margin-top:0.375rem;">Pour Ollama local : http://localhost:11434 · LM Studio : http://localhost:1234</p>
                  </div>
                </details>

              </div>

              <div style="display:flex;justify-content:space-between;margin-top:2rem;">
                <button type="button" class="jb-btn-secondary prev-step" data-prev="1">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg>
                  Retour
                </button>
                <button type="button" class="jb-btn-primary next-step" data-next="3">
                  Suivant : Sources
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
                </button>
              </div>
            </div>

            <!-- === STEP 4: Sources === -->
            <div class="wizard-step" data-step="3" style="display:none;">
              <h3 style="font-size:1.125rem;font-weight:700;color:var(--jb-text);margin-bottom:1.5rem;">Sources à activer</h3>

              <div style="display:grid;gap:0.875rem;">

                <label style="display:flex;align-items:center;justify-content:space-between;padding:1rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);cursor:pointer;gap:1rem;">
                  <div>
                    <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">France Travail</div>
                    <div style="font-size:0.75rem;color:var(--jb-text-muted);">Pôle Emploi — offres publiques, large couverture CDI</div>
                  </div>
                  <input type="checkbox" id="src-france-travail" class="jb-toggle" checked aria-label="Activer France Travail" />
                </label>

                <label style="display:flex;align-items:center;justify-content:space-between;padding:1rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);cursor:pointer;gap:1rem;">
                  <div>
                    <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">Flux RSS de recrutement</div>
                    <div style="font-size:0.75rem;color:var(--jb-text-muted);">Officiel & stable, souvent avec le texte complet — pas de scraping. Flux configurables ensuite dans le tableau de bord local.</div>
                  </div>
                  <input type="checkbox" id="src-rss" class="jb-toggle" checked aria-label="Activer les flux RSS" />
                </label>

                <label style="display:flex;align-items:center;justify-content:space-between;padding:1rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);cursor:pointer;gap:1rem;">
                  <div>
                    <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">Welcome to the Jungle</div>
                    <div style="font-size:0.75rem;color:var(--jb-text-muted);">Startups & scale-ups — très bon pour les profils tech</div>
                  </div>
                  <input type="checkbox" id="src-wttj" class="jb-toggle" checked aria-label="Activer Welcome to the Jungle" />
                </label>

                <label style="display:flex;align-items:center;justify-content:space-between;padding:1rem;border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);cursor:pointer;gap:1rem;">
                  <div>
                    <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">Indeed</div>
                    <div style="font-size:0.75rem;color:var(--jb-text-muted);">Agrégateur généraliste, volume élevé</div>
                  </div>
                  <input type="checkbox" id="src-indeed" class="jb-toggle" aria-label="Activer Indeed" />
                </label>

                <!-- LinkedIn - with disclaimer -->
                <div style="border:1px solid var(--jb-signal);border-radius:var(--jb-radius-input);overflow:hidden;">
                  <label style="display:flex;align-items:center;justify-content:space-between;padding:1rem;cursor:pointer;gap:1rem;background:color-mix(in srgb, var(--jb-signal) 10%, var(--jb-surface));">
                    <div>
                      <div style="font-weight:600;font-size:0.875rem;color:var(--jb-text);">LinkedIn Jobs</div>
                      <div style="font-size:0.75rem;color:var(--jb-text-muted);">Réseau professionnel — le scraping peut être bloqué</div>
                    </div>
                    <input type="checkbox" id="src-linkedin" class="jb-toggle" aria-label="Activer LinkedIn (risque CGU)" />
                  </label>
                  <div style="padding:0.625rem 1rem;background:color-mix(in srgb, var(--jb-signal) 15%, var(--jb-surface));border-top:1px solid var(--jb-signal);">
                    <p style="font-size:0.75rem;color:var(--jb-signal-text);margin:0;">
                      <strong>Attention :</strong> le scraping LinkedIn peut violer leurs CGU. Active cette option à tes risques et périls. Désactivé par défaut.
                    </p>
                  </div>
                </div>

              </div>

              <div style="display:flex;justify-content:space-between;margin-top:2rem;">
                <button type="button" class="jb-btn-secondary prev-step" data-prev="2">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg>
                  Retour
                </button>
                <button type="button" class="jb-btn-primary next-step" data-next="4">
                  Générer ma config
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
                </button>
              </div>
            </div>

            <!-- === STEP 5: Generate === -->
            <div class="wizard-step" data-step="4" style="display:none;">
              <h3 style="font-size:1.125rem;font-weight:700;color:var(--jb-text);margin-bottom:0.5rem;">Ta configuration est prête</h3>
              <p style="font-size:0.8125rem;color:var(--jb-text-muted);margin-bottom:1.5rem;">Télécharge le fichier ou copie la commande pour lancer ton agent.</p>

              <!-- Privacy badge -->
              <div style="display:flex;align-items:center;gap:0.75rem;padding:0.75rem 1rem;background:var(--jb-accent-bg);border:1px solid var(--jb-accent);border-radius:var(--jb-radius-input);margin-bottom:1.5rem;">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="var(--jb-accent-text)" stroke-width="2.5" aria-hidden="true"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                <p style="font-size:0.8125rem;color:var(--jb-accent-text);margin:0;">
                  Ta config reste sur ta machine — rien n'est envoyé.
                  <button id="show-json-toggle" type="button" style="background:none;border:none;color:var(--jb-primary);font-size:0.8125rem;cursor:pointer;text-decoration:underline;padding:0 0.25rem;">Voir ce qu'elle contient</button>
                </p>
              </div>

              <!-- Download button -->
              <button type="button" id="download-config" class="jb-btn-primary" style="width:100%;justify-content:center;margin-bottom:1.25rem;font-size:1rem;padding:0.875rem;">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
                Télécharger jobhunt.config.json
              </button>

              <!-- OS-aware terminal instructions -->
              <div class="jb-card" style="padding:1.25rem;margin-bottom:1.25rem;">
                <h4 style="font-size:0.875rem;font-weight:700;color:var(--jb-text);margin-bottom:1rem;">Lancer ton agent</h4>

                <!-- OS Tabs -->
                <div style="display:flex;gap:0.25rem;margin-bottom:1rem;border-bottom:1px solid var(--jb-border);">
                  <button type="button" class="os-tab active" data-os="mac" style="padding:0.375rem 0.875rem;font-size:0.8125rem;font-weight:600;border:none;background:none;cursor:pointer;border-bottom:2px solid var(--jb-primary);color:var(--jb-primary);">macOS</button>
                  <button type="button" class="os-tab" data-os="linux" style="padding:0.375rem 0.875rem;font-size:0.8125rem;border:none;background:none;cursor:pointer;color:var(--jb-text-muted);border-bottom:2px solid transparent;">Linux</button>
                  <button type="button" class="os-tab" data-os="windows" style="padding:0.375rem 0.875rem;font-size:0.8125rem;border:none;background:none;cursor:pointer;color:var(--jb-text-muted);border-bottom:2px solid transparent;">Windows</button>
                </div>

                <p style="font-size:0.75rem;color:var(--jb-text-muted);margin-bottom:1rem;">
                  Projet pas encore publié sur PyPI : on installe depuis GitHub. Prérequis :
                  <a href="https://www.python.org/downloads/" target="_blank" rel="noopener" style="color:var(--jb-primary);">Python ≥ 3.11</a>,
                  <a href="https://git-scm.com/downloads" target="_blank" rel="noopener" style="color:var(--jb-primary);">git</a> et
                  <a href="https://ollama.com" target="_blank" rel="noopener" style="color:var(--jb-primary);">Ollama</a>.
                </p>

                <div id="os-mac" class="os-panel">
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">1. Installer (une fois)</span>
                      <button type="button" class="copy-cmd" data-cmd={"git clone https://github.com/nathan-pichon/myjobagent.git\ncd myjobagent\npython3 -m venv .venv && source .venv/bin/activate\npip install -e '.[scrape]'\nplaywright install chromium\nollama pull gemma4:e2b"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">git clone https://github.com/nathan-pichon/myjobagent.git
cd myjobagent
python3 -m venv .venv && source .venv/bin/activate
pip install -e '.[scrape]'
playwright install chromium
ollama pull gemma4:e2b</code>
                  </div>
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">2. Importer ta config</span>
                      <button type="button" class="copy-cmd" data-cmd="mja init ~/Downloads/jobhunt.config.json" style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;">mja init ~/Downloads/jobhunt.config.json</code>
                  </div>
                  <div class="command-row">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">3. Lancer</span>
                      <button type="button" class="copy-cmd" data-cmd={"mja doctor\nmja run"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">mja doctor   # vérifie que tout est prêt
mja run</code>
                  </div>
                </div>

                <div id="os-linux" class="os-panel" style="display:none;">
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">1. Installer (une fois)</span>
                      <button type="button" class="copy-cmd" data-cmd={"git clone https://github.com/nathan-pichon/myjobagent.git\ncd myjobagent\npython3 -m venv .venv && source .venv/bin/activate\npip install -e '.[scrape]'\nplaywright install chromium\nollama pull gemma4:e2b"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">git clone https://github.com/nathan-pichon/myjobagent.git
cd myjobagent
python3 -m venv .venv && source .venv/bin/activate
pip install -e '.[scrape]'
playwright install chromium
ollama pull gemma4:e2b</code>
                  </div>
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">2. Importer ta config</span>
                      <button type="button" class="copy-cmd" data-cmd="mja init ~/Downloads/jobhunt.config.json" style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;">mja init ~/Downloads/jobhunt.config.json</code>
                  </div>
                  <div class="command-row">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">3. Lancer</span>
                      <button type="button" class="copy-cmd" data-cmd={"mja doctor\nmja run"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">mja doctor   # vérifie que tout est prêt
mja run</code>
                  </div>
                </div>

                <div id="os-windows" class="os-panel" style="display:none;">
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">1. Installer (PowerShell, une fois)</span>
                      <button type="button" class="copy-cmd" data-cmd={"git clone https://github.com/nathan-pichon/myjobagent.git\ncd myjobagent\npython -m venv .venv; .venv\\Scripts\\activate\npip install -e \".[scrape]\"\nplaywright install chromium\nollama pull gemma4:e2b"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">git clone https://github.com/nathan-pichon/myjobagent.git
cd myjobagent
python -m venv .venv; .venv\Scripts\activate
pip install -e ".[scrape]"
playwright install chromium
ollama pull gemma4:e2b</code>
                  </div>
                  <div class="command-row" style="margin-bottom:0.75rem;">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">2. Importer ta config</span>
                      <button type="button" class="copy-cmd" data-cmd="mja init %USERPROFILE%\\Downloads\\jobhunt.config.json" style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;">mja init %USERPROFILE%\Downloads\jobhunt.config.json</code>
                  </div>
                  <div class="command-row">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">3. Lancer</span>
                      <button type="button" class="copy-cmd" data-cmd={"mja doctor\nmja run"} style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.625rem 0.875rem;white-space:pre;line-height:1.6;">mja doctor
mja run</code>
                  </div>
                </div>

                <p style="font-size:0.75rem;color:var(--jb-text-muted);margin-top:1rem;">
                  Quand la chasse est terminée, ton dashboard local s'ouvre automatiquement sur <code style="font-family:monospace;font-size:0.6875rem;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">http://localhost:4321</code>.
                </p>

                <div style="margin-top:1rem;padding:0.875rem 1rem;background:color-mix(in srgb, var(--jb-signal) 10%, var(--jb-surface));border:1px solid var(--jb-signal);border-radius:var(--jb-radius-input);">
                  <p style="font-size:0.75rem;color:var(--jb-text);margin:0 0 0.5rem;font-weight:600;">↻ Relancer plus tard (nouveau terminal)</p>
                  <p style="font-size:0.75rem;color:var(--jb-text-muted);margin:0 0 0.5rem;line-height:1.5;">
                    La commande <code style="font-family:monospace;font-size:0.6875rem;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">mja</code> vit dans l'environnement du projet. Dans un nouveau terminal, réactive-le d'abord :
                  </p>
                  <div class="command-row">
                    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.25rem;">
                      <span style="font-size:0.625rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">macOS / Linux</span>
                      <button type="button" class="copy-cmd" data-cmd={"cd myjobagent\nsource .venv/bin/activate\nmja run"} style="font-size:0.625rem;padding:0.15rem 0.45rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                    </div>
                    <code class="code-block" style="display:block;padding:0.5rem 0.75rem;white-space:pre;line-height:1.6;font-size:0.6875rem;">cd myjobagent
source .venv/bin/activate
mja run</code>
                  </div>
                  <p style="font-size:0.6875rem;color:var(--jb-text-muted);margin:0.6rem 0 0;line-height:1.5;">
                    Sur Windows : <code style="font-family:monospace;font-size:0.625rem;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">.venv\Scripts\activate</code> à la place de la 2ᵉ ligne.
                    <br/>Astuce : pour taper <code style="font-family:monospace;font-size:0.625rem;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">mja</code> de n'importe où sans activer le venv, ajoute un alias dans ton shell —
                    <code style="font-family:monospace;font-size:0.625rem;background:var(--jb-surface-alt);padding:0.05em 0.3em;border-radius:3px;">alias mja="~/myjobagent/.venv/bin/mja"</code>.
                  </p>
                </div>
              </div>

              <!-- Base64 variant (collapsible) -->
              <details style="border:1px solid var(--jb-border);border-radius:var(--jb-radius-input);overflow:hidden;margin-bottom:1.25rem;">
                <summary style="padding:0.875rem 1rem;cursor:pointer;font-size:0.8125rem;font-weight:600;color:var(--jb-text);list-style:none;display:flex;align-items:center;justify-content:space-between;">
                  Variante tout-en-un (sans téléchargement)
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
                </summary>
                <div style="padding:1rem;border-top:1px solid var(--jb-border);">
                  <p style="font-size:0.75rem;color:var(--jb-text-muted);margin-bottom:0.75rem;">Colle directement ta config inline dans la commande (pratique pour config courte). Si ta config dépasse ~2 ko, préfère le fichier.</p>
                  <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.375rem;">
                    <span style="font-size:0.6875rem;font-weight:600;color:var(--jb-text-muted);text-transform:uppercase;letter-spacing:0.05em;">Commande base64url</span>
                    <button type="button" id="copy-b64-cmd" style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
                  </div>
                  <code id="b64-cmd-display" class="code-block" style="display:block;padding:0.625rem 0.875rem;word-break:break-all;font-size:0.6875rem;">mja init --b64 <em style="color:var(--jb-text-muted);">(génération en cours…)</em></code>
                </div>
              </details>

              <!-- JSON preview -->
              <div id="json-preview-wrapper" style="display:none;margin-top:0.5rem;">
                <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.5rem;">
                  <span style="font-size:0.8125rem;font-weight:600;color:var(--jb-text);">Contenu de la config (JSON)</span>
                  <button type="button" id="copy-json-btn" style="font-size:0.6875rem;padding:0.2rem 0.625rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier JSON</button>
                </div>
                <pre id="json-preview" class="code-block" style="padding:1rem;overflow:auto;max-height:20rem;font-size:0.6875rem;line-height:1.65;white-space:pre;"></pre>
              </div>

              <button type="button" class="jb-btn-secondary prev-step" data-prev="3" style="margin-top:1rem;">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg>
                Retour
              </button>
            </div>

          </div><!-- /wizard-form -->

          <!-- Live JSON preview (desktop only) -->
          <div id="json-sidebar" class="jb-card" style="display:none;padding:1.25rem;height:fit-content;position:sticky;top:4.5rem;">
            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.875rem;">
              <span style="font-size:0.8125rem;font-weight:700;color:var(--jb-text);">Aperçu live</span>
              <button type="button" id="sidebar-copy-json" style="font-size:0.6875rem;padding:0.2rem 0.5rem;border:1px solid var(--jb-border);border-radius:4px;background:var(--jb-surface);color:var(--jb-text-muted);cursor:pointer;">Copier</button>
            </div>
            <pre id="sidebar-json" style="font-family:'JetBrains Mono','Fira Code',monospace;font-size:0.6875rem;line-height:1.6;color:var(--jb-text);overflow:auto;max-height:65vh;white-space:pre;margin:0;"></pre>
          </div>

        </div><!-- /wizard-columns -->
      </div><!-- /wizard-app -->
    </section>
  </main>

  <!-- ===================== FOOTER ===================== -->
  <footer style="border-top:1px solid var(--jb-border);padding:1.5rem 1.25rem;text-align:center;">
    <p style="font-size:0.8125rem;color:var(--jb-text-muted);">
      MyJobAgent — Open Source · Zéro analytics · Tout reste sur ta machine
    </p>
  </footer>
</Layout>

<script>
// ============================================================
//  MyJobAgent Configurator — 100% client-side, zero server
// ============================================================

// ---- STATE ----
const state = {
  step: 0,
  profile: {
    roles: [],
    stack_core: [],
    stack_nice: [],
    exclusions: [],
    geography: [],
    contract_types: [],
    seniority: 'Senior',
    years_experience: null,
    min_salary: null,
    min_tjm: null,
  },
  search: {
    modes: ['PLATFORM', 'CAREERS'],
    platforms: ['welcometothejungle.com', 'free-work.com', 'malt.fr', 'talent.io'],
    max_steps: 200,
    max_results_per_query: 25,
  },
  llm: {
    provider: 'ollama',
    model: 'gemma4:e2b',
    base_url: 'http://localhost:11434',
    temperature: 0.3,
    api_key_env: 'JOBHUNT_LLM_API_KEY',
  },
  sources: {
    france_travail_enabled: true,
    welcometothejungle_enabled: true,
    linkedin_enabled: false,
    indeed_enabled: false,
    rss_enabled: true,
    rss_feeds: [
      { name: 'WeWorkRemotely — Backend', url: 'https://weworkremotely.com/categories/remote-back-end-programming-jobs.rss', enabled: true },
      { name: 'WeWorkRemotely — Full-Stack', url: 'https://weworkremotely.com/categories/remote-full-stack-programming-jobs.rss', enabled: true },
      { name: 'Remotive — Software Dev', url: 'https://remotive.com/remote-jobs/feed/software-dev', enabled: true },
      { name: 'RemoteOK', url: 'https://remoteok.com/remote-dev-jobs.rss', enabled: true },
      { name: 'Himalayas — Software', url: 'https://himalayas.app/jobs/rss?category=software-engineering', enabled: true },
    ],
  },
  scoring: { threshold: 50 },
};

// ---- RESTORE from localStorage ----
function saveState() {
  try { localStorage.setItem('jb_wizard_state', JSON.stringify(state)); } catch(e) {}
}

function restoreState() {
  try {
    const saved = localStorage.getItem('jb_wizard_state');
    if (!saved) return;
    const parsed = JSON.parse(saved);
    Object.assign(state.profile, parsed.profile || {});
    Object.assign(state.search, parsed.search || {});
    Object.assign(state.llm, parsed.llm || {});
    Object.assign(state.sources, parsed.sources || {});
    Object.assign(state.scoring, parsed.scoring || {});
  } catch(e) {}
}

// ---- TAG INPUT HELPER ----
function initTagInput(inputId, addBtnId, tagsId, stateArray, stateKey) {
  const input = document.getElementById(inputId);
  const addBtn = document.getElementById(addBtnId);
  const container = document.getElementById(tagsId);

  if (!input || !addBtn || !container) return;

  function renderTags() {
    container.innerHTML = '';
    stateArray.forEach((tag, i) => {
      const span = document.createElement('span');
      span.className = 'jb-tag';
      span.innerHTML = `${tag}<button class="jb-tag-remove" aria-label="Supprimer ${tag}" data-idx="${i}">&times;</button>`;
      container.appendChild(span);
    });
    updateJSON();
    saveState();
  }

  function addTag() {
    const val = input.value.trim();
    if (val && !stateArray.includes(val)) {
      stateArray.push(val);
      renderTags();
    }
    input.value = '';
    input.focus();
  }

  addBtn.addEventListener('click', addTag);
  input.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') { e.preventDefault(); addTag(); }
  });

  container.addEventListener('click', (e) => {
    const btn = (e.target as HTMLElement).closest('.jb-tag-remove');
    if (btn) {
      const idx = parseInt((btn as HTMLElement).dataset.idx || '0', 10);
      stateArray.splice(idx, 1);
      renderTags();
    }
  });

  // Render initial tags from state (restored from localStorage)
  renderTags();
}

// ---- BUILD CONFIG JSON ----
function buildConfig() {
  const cfg: Record<string, any> = {
    schema_version: '1.0',
    profile: { ...state.profile },
    llm: { ...state.llm },
    search: { ...state.search },
    sources: { ...state.sources },
    scoring: { ...state.scoring },
    filters: {
      domain_blacklist: [],
      reject_url_patterns: [],
      fasttrack_job_patterns: [],
    },
  };
  // Remove null optional fields from profile
  if (!cfg.profile.years_experience) delete cfg.profile.years_experience;
  if (!cfg.profile.min_salary) delete cfg.profile.min_salary;
  if (!cfg.profile.min_tjm) delete cfg.profile.min_tjm;
  return cfg;
}

// ---- UPDATE JSON DISPLAY ----
function updateJSON() {
  const cfg = buildConfig();
  const pretty = JSON.stringify(cfg, null, 2);

  const sidebar = document.getElementById('sidebar-json');
  if (sidebar) sidebar.textContent = pretty;

  const preview = document.getElementById('json-preview');
  if (preview) preview.textContent = pretty;

  // Update base64url command
  const b64 = btoa(unescape(encodeURIComponent(JSON.stringify(cfg))))
    .replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  const b64Display = document.getElementById('b64-cmd-display');
  if (b64Display) b64Display.textContent = `mja init --b64 ${b64}`;

  const copyB64 = document.getElementById('copy-b64-cmd');
  if (copyB64) {
    (copyB64 as HTMLElement & { _b64: string })._b64 = `mja init --b64 ${b64}`;
  }
}

// ---- STEP NAVIGATION ----
function goToStep(targetStep: number) {
  // Hide all steps
  document.querySelectorAll<HTMLElement>('.wizard-step').forEach(s => {
    s.style.display = 'none';
  });
  // Show target step
  const target = document.querySelector<HTMLElement>(`.wizard-step[data-step="${targetStep}"]`);
  if (target) target.style.display = '';

  // Update step buttons
  document.querySelectorAll<HTMLElement>('.step-btn').forEach(btn => {
    const s = parseInt(btn.dataset.step || '0', 10);
    const numEl = btn.querySelector('.step-num') as HTMLElement;
    btn.removeAttribute('aria-current');
    if (s === targetStep) {
      btn.style.borderColor = 'var(--jb-primary)';
      btn.style.background = 'var(--jb-primary-soft)';
      btn.style.color = 'var(--jb-primary)';
      if (numEl) { numEl.style.background = 'var(--jb-primary)'; numEl.style.color = 'white'; }
      btn.setAttribute('aria-current', 'step');
    } else if (s < targetStep) {
      btn.style.borderColor = 'var(--jb-accent)';
      btn.style.background = 'var(--jb-accent-bg)';
      btn.style.color = 'var(--jb-accent-text)';
      if (numEl) { numEl.style.background = 'var(--jb-accent-text)'; numEl.style.color = 'white'; }
    } else {
      btn.style.borderColor = 'var(--jb-border)';
      btn.style.background = 'var(--jb-surface)';
      btn.style.color = 'var(--jb-text-muted)';
      if (numEl) { numEl.style.background = 'var(--jb-border)'; numEl.style.color = 'var(--jb-text-muted)'; }
    }
  });

  state.step = targetStep;

  // Show sidebar JSON only from step 2+
  const sidebar = document.getElementById('json-sidebar');
  if (sidebar) sidebar.style.display = targetStep >= 1 ? '' : 'none';

  // Update columns layout on wide screens
  updateColumnsLayout();
  updateJSON();

  // Scroll wizard into view smoothly
  document.getElementById('wizard')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
}

function updateColumnsLayout() {
  const cols = document.getElementById('wizard-columns');
  if (!cols) return;
  if (window.innerWidth >= 900 && state.step >= 1) {
    cols.style.gridTemplateColumns = '1fr minmax(280px, 340px)';
  } else {
    cols.style.gridTemplateColumns = '1fr';
  }
}

// ---- OS DETECTION ----
function detectOS() {
  const p = navigator.platform?.toLowerCase() || '';
  const ua = navigator.userAgent?.toLowerCase() || '';
  if (p.includes('win') || ua.includes('windows')) return 'windows';
  if (p.includes('linux') || ua.includes('linux')) return 'linux';
  return 'mac'; // default macOS
}

function activateOsTab(os: string) {
  document.querySelectorAll<HTMLElement>('.os-tab').forEach(t => {
    if (t.dataset.os === os) {
      t.style.borderBottomColor = 'var(--jb-primary)';
      t.style.color = 'var(--jb-primary)';
      t.style.fontWeight = '600';
    } else {
      t.style.borderBottomColor = 'transparent';
      t.style.color = 'var(--jb-text-muted)';
      t.style.fontWeight = '';
    }
  });
  document.querySelectorAll<HTMLElement>('.os-panel').forEach(p => {
    p.style.display = p.id === `os-${os}` ? '' : 'none';
  });
}

// ---- COPY HELPER ----
async function copyText(text: string, btn: HTMLElement) {
  try {
    await navigator.clipboard.writeText(text);
    const orig = btn.textContent;
    btn.textContent = 'Copié !';
    btn.style.color = 'var(--jb-accent-text)';
    setTimeout(() => { btn.textContent = orig; btn.style.color = ''; }, 1800);
  } catch(e) {
    // Fallback
    const ta = document.createElement('textarea');
    ta.value = text;
    ta.style.position = 'fixed';
    ta.style.opacity = '0';
    document.body.appendChild(ta);
    ta.select();
    document.execCommand('copy');
    document.body.removeChild(ta);
    const orig = btn.textContent;
    btn.textContent = 'Copié !';
    setTimeout(() => { btn.textContent = orig; }, 1800);
  }
}

// ---- DOWNLOAD ----
function downloadConfig() {
  const cfg = buildConfig();
  const json = JSON.stringify(cfg, null, 2);
  const blob = new Blob([json], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'jobhunt.config.json';
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  URL.revokeObjectURL(url);
}

// ---- LLM DEFAULTS ----
const LLM_DEFAULTS: Record<string, { model: string; base_url: string }> = {
  ollama:    { model: 'gemma4:e2b',             base_url: 'http://localhost:11434' },
  lmstudio:  { model: 'local-model',             base_url: 'http://localhost:1234' },
  openai:    { model: 'gpt-4o-mini',             base_url: 'https://api.openai.com/v1' },
  anthropic: { model: 'claude-haiku-4-5',       base_url: 'https://api.anthropic.com' },
  mistral:   { model: 'mistral-small-latest',    base_url: 'https://api.mistral.ai/v1' },
  groq:      { model: 'llama-3.1-8b-instant',   base_url: 'https://api.groq.com/openai/v1' },
};

const MODEL_HINTS: Record<string, string> = {
  ollama:    'Pour Ollama : gemma4:e2b (léger), gemma4:e4b, qwen2.5:7b',
  lmstudio:  'Entrez le nom du modèle chargé dans LM Studio',
  openai:    'Ex : gpt-4o-mini, gpt-4o, gpt-3.5-turbo',
  anthropic: 'Ex : claude-haiku-4-5, claude-sonnet-4-5',
  mistral:   'Ex : mistral-small-latest, mistral-medium-latest',
  groq:      'Ex : llama-3.1-8b-instant, mixtral-8x7b-32768',
};

// ============================================================
//  INIT
// ============================================================
document.addEventListener('DOMContentLoaded', () => {
  // Restore from localStorage
  restoreState();

  // Initialize tag inputs
  initTagInput('roles-input', 'roles-add', 'roles-tags', state.profile.roles, 'roles');
  initTagInput('stack-core-input', 'stack-core-add', 'stack-core-tags', state.profile.stack_core, 'stack_core');
  initTagInput('stack-nice-input', 'stack-nice-add', 'stack-nice-tags', state.profile.stack_nice, 'stack_nice');
  initTagInput('exclusions-input', 'exclusions-add', 'exclusions-tags', state.profile.exclusions, 'exclusions');
  initTagInput('geography-input', 'geography-add', 'geography-tags', state.profile.geography, 'geography');
  initTagInput('platforms-input', 'platforms-add', 'platforms-tags', state.search.platforms, 'platforms');

  // Restore form fields from state
  const seniorityEl = document.getElementById('seniority-select') as HTMLSelectElement;
  if (seniorityEl) { seniorityEl.value = state.profile.seniority; }

  const yearsEl = document.getElementById('years-exp') as HTMLInputElement;
  if (yearsEl && state.profile.years_experience) yearsEl.value = String(state.profile.years_experience);

  const salaryEl = document.getElementById('min-salary') as HTMLInputElement;
  if (salaryEl && state.profile.min_salary) salaryEl.value = String(state.profile.min_salary);

  const tjmEl = document.getElementById('min-tjm') as HTMLInputElement;
  if (tjmEl && state.profile.min_tjm) tjmEl.value = String(state.profile.min_tjm);

  // Contract checkboxes
  document.querySelectorAll<HTMLInputElement>('.contract-cb').forEach(cb => {
    cb.checked = state.profile.contract_types.includes(cb.dataset.contract || '');
    cb.addEventListener('change', () => {
      const contract = cb.dataset.contract || '';
      if (cb.checked && !state.profile.contract_types.includes(contract)) {
        state.profile.contract_types.push(contract);
      } else if (!cb.checked) {
        state.profile.contract_types = state.profile.contract_types.filter(c => c !== contract);
      }
      updateJSON(); saveState();
    });
  });

  // Seniority
  if (seniorityEl) {
    seniorityEl.addEventListener('change', () => { state.profile.seniority = seniorityEl.value; updateJSON(); saveState(); });
  }

  // Years
  if (yearsEl) {
    yearsEl.addEventListener('input', () => { state.profile.years_experience = parseInt(yearsEl.value) || null; updateJSON(); saveState(); });
  }

  // Salary
  if (salaryEl) {
    salaryEl.addEventListener('input', () => { state.profile.min_salary = parseInt(salaryEl.value) || null; updateJSON(); saveState(); });
  }

  // TJM
  if (tjmEl) {
    tjmEl.addEventListener('input', () => { state.profile.min_tjm = parseInt(tjmEl.value) || null; updateJSON(); saveState(); });
  }

  // Search modes
  document.querySelectorAll<HTMLInputElement>('.mode-cb').forEach(cb => {
    const mode = cb.dataset.mode as 'PLATFORM' | 'CAREERS' | 'LINKEDIN';
    cb.checked = state.search.modes.includes(mode);
    cb.addEventListener('change', () => {
      if (cb.checked && !state.search.modes.includes(mode)) {
        state.search.modes.push(mode);
      } else if (!cb.checked) {
        state.search.modes = state.search.modes.filter(m => m !== mode);
      }
      updateJSON(); saveState();
    });
  });

  // Score threshold
  const slider = document.getElementById('score-threshold') as HTMLInputElement;
  const display = document.getElementById('threshold-display');
  if (slider) {
    slider.value = String(state.scoring.threshold);
    if (display) display.textContent = String(state.scoring.threshold);
    slider.addEventListener('input', () => {
      state.scoring.threshold = parseInt(slider.value, 10);
      if (display) display.textContent = slider.value;
      updateJSON(); saveState();
    });
  }

  // LLM provider radio cards
  document.querySelectorAll<HTMLInputElement>('.llm-provider-radio').forEach(radio => {
    radio.checked = radio.value === state.llm.provider;
    radio.addEventListener('change', () => {
      if (!radio.checked) return;
      const provider = radio.value as keyof typeof LLM_DEFAULTS;
      state.llm.provider = provider;
      const defaults = LLM_DEFAULTS[provider];
      if (defaults) {
        state.llm.model = defaults.model;
        state.llm.base_url = defaults.base_url;
        const modelEl = document.getElementById('llm-model') as HTMLInputElement;
        const urlEl = document.getElementById('llm-base-url') as HTMLInputElement;
        const hintEl = document.getElementById('llm-model-hint');
        if (modelEl) modelEl.value = defaults.model;
        if (urlEl) urlEl.value = defaults.base_url;
        if (hintEl) hintEl.textContent = MODEL_HINTS[provider] || '';
      }
      // Update card styles
      document.querySelectorAll<HTMLElement>('.llm-card').forEach(card => {
        if (card.dataset.provider === provider) {
          card.style.borderColor = 'var(--jb-primary)';
          card.style.background = 'var(--jb-primary-soft)';
        } else {
          card.style.borderColor = 'var(--jb-border)';
          card.style.background = 'var(--jb-surface)';
        }
      });
      updateJSON(); saveState();
    });
  });

  // LLM model input
  const modelInput = document.getElementById('llm-model') as HTMLInputElement;
  if (modelInput) {
    modelInput.value = state.llm.model;
    modelInput.addEventListener('input', () => { state.llm.model = modelInput.value; updateJSON(); saveState(); });
  }

  const urlInput = document.getElementById('llm-base-url') as HTMLInputElement;
  if (urlInput) {
    urlInput.value = state.llm.base_url;
    urlInput.addEventListener('input', () => { state.llm.base_url = urlInput.value; updateJSON(); saveState(); });
  }

  // Sources toggles
  const srcFT = document.getElementById('src-france-travail') as HTMLInputElement;
  const srcRss = document.getElementById('src-rss') as HTMLInputElement;
  const srcWTTJ = document.getElementById('src-wttj') as HTMLInputElement;
  const srcIndeed = document.getElementById('src-indeed') as HTMLInputElement;
  const srcLinkedIn = document.getElementById('src-linkedin') as HTMLInputElement;

  if (srcRss) { srcRss.checked = state.sources.rss_enabled; srcRss.addEventListener('change', () => { state.sources.rss_enabled = srcRss.checked; updateJSON(); saveState(); }); }
  if (srcFT) { srcFT.checked = state.sources.france_travail_enabled; srcFT.addEventListener('change', () => { state.sources.france_travail_enabled = srcFT.checked; updateJSON(); saveState(); }); }
  if (srcWTTJ) { srcWTTJ.checked = state.sources.welcometothejungle_enabled; srcWTTJ.addEventListener('change', () => { state.sources.welcometothejungle_enabled = srcWTTJ.checked; updateJSON(); saveState(); }); }
  if (srcIndeed) { srcIndeed.checked = state.sources.indeed_enabled; srcIndeed.addEventListener('change', () => { state.sources.indeed_enabled = srcIndeed.checked; updateJSON(); saveState(); }); }
  if (srcLinkedIn) { srcLinkedIn.checked = state.sources.linkedin_enabled; srcLinkedIn.addEventListener('change', () => { state.sources.linkedin_enabled = srcLinkedIn.checked; updateJSON(); saveState(); }); }

  // Step navigation buttons
  document.querySelectorAll<HTMLButtonElement>('.step-btn').forEach(btn => {
    btn.addEventListener('click', () => goToStep(parseInt(btn.dataset.step || '0', 10)));
  });

  document.querySelectorAll<HTMLButtonElement>('.next-step').forEach(btn => {
    btn.addEventListener('click', () => goToStep(parseInt(btn.dataset.next || '0', 10)));
  });

  document.querySelectorAll<HTMLButtonElement>('.prev-step').forEach(btn => {
    btn.addEventListener('click', () => goToStep(parseInt(btn.dataset.prev || '0', 10)));
  });

  // Suggestion buttons
  const tagTargets: Record<string, string[]> = {
    'roles': state.profile.roles,
    'stack-core': state.profile.stack_core,
    'geography': state.profile.geography,
    'platforms': state.search.platforms,
  };

  const tagContainers: Record<string, string> = {
    'roles': 'roles-tags',
    'stack-core': 'stack-core-tags',
    'geography': 'geography-tags',
    'platforms': 'platforms-tags',
  };

  document.querySelectorAll<HTMLButtonElement>('.suggestion-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const target = btn.dataset.target || '';
      const value = btn.dataset.value || '';
      const arr = tagTargets[target];
      const containerId = tagContainers[target];
      if (arr && value && !arr.includes(value)) {
        arr.push(value);
        // Re-render tags for this container
        const container = document.getElementById(containerId);
        if (container) {
          container.innerHTML = '';
          arr.forEach((tag, i) => {
            const span = document.createElement('span');
            span.className = 'jb-tag';
            span.innerHTML = `${tag}<button class="jb-tag-remove" aria-label="Supprimer ${tag}" data-idx="${i}">&times;</button>`;
            container.appendChild(span);
          });
          container.addEventListener('click', (e) => {
            const removeBtn = (e.target as HTMLElement).closest('.jb-tag-remove');
            if (removeBtn) {
              const idx = parseInt((removeBtn as HTMLElement).dataset.idx || '0', 10);
              arr.splice(idx, 1);
              const ev = new Event('_rerender');
              container.dispatchEvent(ev);
            }
          });
        }
        updateJSON(); saveState();
      }
    });
  });

  // Download button
  document.getElementById('download-config')?.addEventListener('click', downloadConfig);

  // Copy JSON button (preview area)
  document.getElementById('copy-json-btn')?.addEventListener('click', (e) => {
    copyText(JSON.stringify(buildConfig(), null, 2), e.currentTarget as HTMLElement);
  });

  // Sidebar copy
  document.getElementById('sidebar-copy-json')?.addEventListener('click', (e) => {
    copyText(JSON.stringify(buildConfig(), null, 2), e.currentTarget as HTMLElement);
  });

  // Copy command buttons
  document.querySelectorAll<HTMLElement>('.copy-cmd').forEach(btn => {
    btn.addEventListener('click', () => copyText(btn.dataset.cmd || '', btn));
  });

  // Copy base64 command
  document.getElementById('copy-b64-cmd')?.addEventListener('click', (e) => {
    const btn = e.currentTarget as HTMLElement & { _b64: string };
    copyText(btn._b64 || document.getElementById('b64-cmd-display')?.textContent || '', btn);
  });

  // Show JSON toggle (privacy disclosure)
  document.getElementById('show-json-toggle')?.addEventListener('click', (e) => {
    const wrapper = document.getElementById('json-preview-wrapper');
    if (!wrapper) return;
    const isHidden = wrapper.style.display === 'none';
    wrapper.style.display = isHidden ? '' : 'none';
    (e.currentTarget as HTMLElement).textContent = isHidden ? 'Masquer le JSON' : 'voir ce qu\'elle contient';
    updateJSON();
  });

  // OS tabs
  document.querySelectorAll<HTMLElement>('.os-tab').forEach(tab => {
    tab.addEventListener('click', () => activateOsTab(tab.dataset.os || 'mac'));
  });

  // Responsive: adjust layout on resize
  window.addEventListener('resize', updateColumnsLayout);

  // Detect OS and set active tab
  const detectedOS = detectOS();
  activateOsTab(detectedOS);

  // Initial render
  updateColumnsLayout();
  updateJSON();
  goToStep(state.step);
});
</script>

<style>
/* Responsive wizard columns */
@media (min-width: 900px) {
  #wizard-columns.wide {
    grid-template-columns: 1fr minmax(280px, 340px);
  }
}

/* Ensure sidebar JSON is monospace */
#sidebar-json, #json-preview {
  font-family: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
}
</style>
</Layout>
